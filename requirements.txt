# OCR screen-capture transcription tool — runtime dependencies
# Verified on Python 3.14 (Windows).
rapidocr>=3.9.0          # PP-OCRv6 multilingual models bundled (det+rec+cls)
onnxruntime>=1.27.0
opencv-python>=4.13.0
numpy>=2.5.0
Pillow>=12.0.0
pystray>=0.19.5
pyperclip>=1.11.0
pyttsx3>=2.99
pywin32>=311
requests>=2.34.0
flask>=3.0
pywebview>=6.2     # native window UI (Edge WebView2 on Windows)

# Build only (exe packaging):
# pyinstaller>=6.0
