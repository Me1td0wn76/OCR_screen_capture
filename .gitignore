*.pyc
__pycache__/
venv/
.venv/

build/
dist/

# Runtime / user files
ocr_tool.log
config.json

# OCR models are downloaded at runtime by the setup wizard; keep them out of git.
# (The build bundles the Japanese model only if it happens to be present locally.)
models/

# Built installer (publish it as a GitHub Release asset, not in git).
installer_out/

# Local scratch / editor
*.spec.bak
.vscode/
